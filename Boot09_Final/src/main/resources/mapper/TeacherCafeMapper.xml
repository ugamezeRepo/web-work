<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tcafe">
	<update id="update" parameterType="tcafeDto">
		UPDATE board_cafe
		SET title=#{title}, content=#{content}
		WHERE num=#{num}
	</update>

	<insert id="insert" parameterType="cafeDto">
		INSERT INTO board_cafe
		(num, writer, title, content, viewCount, regdate)
		VALUES (board_cafe_seq.NEXTVAL, #{writer}, #{title}, #{content}, 0, SYSDATE)
	</insert>

	<select id="getData" parameterType="int" resultType="tcafeDto">
		SELECT num, writer, title, content, viewCount, regdate
		FROM board_cafe
		WHERE num=#{num}
	</select>

	<select id="getList" parameterType="tcafeDto" resultType="tcafeDto">
		SELECT *
		FROM
			(SELECT result1.*, ROWNUM AS rnum
			FROM
				(SELECT num, writer, title, content, viewCount, TO_CHAR(regdate, 'YYYY.MM.DD HH24:MI') AS regdate
				FROM board_cafe
				ORDER BY num DESC) result1)
		WHERE rnum BETWEEN #{startRowNum} AND #{endRowNum}
	</select>
	
	<select id="getCount" resultType="int" >
		SELECT NVL(MAX(ROWNUM), 0)
		FROM board_cafe
	</select>
	
	<delete id="delete" parameterType="int">
		DELETE FROM board_cafe
		WHERE num=#{num}
	</delete>
</mapper>